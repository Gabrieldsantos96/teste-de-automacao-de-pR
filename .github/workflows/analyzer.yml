name: PR Analyzer

on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize]

jobs:
  analyze-pr:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout the base branch (main) with full history
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          ref: main
          fetch-depth: 0

      # 2️⃣ Fetch the PR head
      - name: Fetch PR head
        run: git fetch origin ${{ github.event.pull_request.head.sha }}:pr-head

      # 3️⃣ Setup Node.js (if still needed; appears unused in current workflow—consider removing if not required)
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      # 4️⃣ Listar arquivos modificados (compare main to pr-head)
      - name: Get modified files
        id: files
        run: |
          FILES=$(git diff --name-only main...pr-head --diff-filter=ACM)
          echo "files<<EOF" >> $GITHUB_OUTPUT
          echo "$FILES" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      # 5️⃣ Coletar diffs completos (compare main to pr-head)
      - name: Collect file diffs
        id: diffs
        run: |
          DIFFS=$(git diff main...pr-head)
          echo "diffs<<EOF" >> $GITHUB_OUTPUT
          echo "$DIFFS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      # 6️⃣ Enviar diffs para OpenRouter (ChatGPT)
      - name: Send to OpenRouter
        id: chatgpt
        env:
          FILES: ${{ steps.files.outputs.files }}
          DIFFS: ${{ steps.diffs.outputs.diffs }}
          AI_AGENT: ${{ secrets.AI_AGENT }}
        run: |
          RESPONSE=$(curl -s https://openrouter.ai/api/v1/chat/completions \
            -H "Authorization: Bearer $AI_AGENT" \
            -H "Content-Type: application/json" \
            -d "{
              \"model\": \"openai/gpt-4o-mini\",
              \"messages\": [
                {\"role\": \"system\", \"content\": \"Você é um analista de Pull Requests. Gere relatórios técnicos detalhados de forma clara e estruturada, incluindo resumo, novas features, impacto no sistema, possíveis melhorias de código, pontos de atenção em bugs/performance e um diagrama textual em Mermaid.\"},
                {\"role\": \"user\", \"content\": \"Analise este Pull Request e gere um relatório completo.\n\nArquivos modificados:\n$FILES\n\nDiffs:\n$DIFFS\"}
              ]
            }" | jq -r '.choices[0].message.content')

          echo "report<<EOF" >> $GITHUB_OUTPUT
          echo "$RESPONSE" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      # 7️⃣ Publicar relatório como comentário no PR
      - name: Post PR Analysis as comment
        uses: peter-evans/create-or-update-comment@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            ## 🤖 PR Analyzer Report
            ${{ steps.chatgpt.outputs.report }}